version: 3
automerge: false

autodiscover:
  mode: enabled

projects:
- dir: provisioning/baseline_image
  workflow: lxc-instance

workflows:
  lxc-instance:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
        - run: ansible-playbook deploy_*.yml
