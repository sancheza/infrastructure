version: 3
automerge: false

autodiscover:
  mode: enabled

projects:
- dir: services/vault-provision
  workflow: default

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
        - run: ansible-playbook deploy_*.yml
