version: 3
automerge: false

autodiscover:
  mode: auto

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
        - run: ansible-playbook deploy_*.yml
